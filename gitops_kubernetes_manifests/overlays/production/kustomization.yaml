resources:
  - ../../base
  - namespace.yaml

namespace: production

patches:
  - patch: patches/go-deployment-patch.yaml

configMapGenerator:
  - name: go-envs
    envs:
      - envs/go.env

secretGenerator:
  - name: go-secrets
    envs:
      - secrets/go.secret